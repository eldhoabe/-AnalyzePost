// Backend wiring lands in a later milestone (issue #11). This stub keeps
// the service worker installable and the build pipeline green.
console.debug("[LinkedIn Signal] background service worker loaded");
